import { TextcutterPipe } from './textcutter.pipe';

describe('TextcutterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextcutterPipe();
    expect(pipe).toBeTruthy();
  });
});
